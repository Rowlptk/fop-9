#include<stdio.h>

float mul(float x, float y); // Parameterized function

void main()
{
    printf("Multiplication = %f",mul(1.2,2.3));

}

float mul(float x, float y)
{
    return (x*y);
}

// Next class merge fop-10 ==> Sunday, Tuesday, Thursday==> 3:00 to 4:30
pattern, array, string, function
